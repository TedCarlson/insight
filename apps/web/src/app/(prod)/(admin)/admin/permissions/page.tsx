export default function AdminPermissionsPage() {
  return (
    <div className="mt-6 rounded-2xl border border-[var(--to-border)] bg-[var(--to-surface)] p-4">
      <div className="text-sm font-semibold">Permissions (v2 placeholder)</div>
      <div className="mt-1 text-sm text-[var(--to-ink-muted)]">
        This will become the grant delegation console (unit-scoped grants like roster/planning/uploads).
      </div>
    </div>
  );
}
