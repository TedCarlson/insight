export default function TechMyWorkPage() {
  return (
    <div className="mt-6 rounded-2xl border border-[var(--to-border)] bg-[var(--to-surface)] p-4">
      <div className="text-sm font-semibold">My Work (v2 placeholder)</div>
      <div className="mt-1 text-sm text-[var(--to-ink-muted)]">
        Next step: show technician schedule, assignments, and limited “new/create” actions (self-only).
      </div>
    </div>
  );
}
